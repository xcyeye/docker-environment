services:
  web:
    image: nginx:latest
    volumes:
      - type: volume
        source: nginx-data
        target: /app
volumes:
  nginx-data:
    name: nginx-data-volume
    driver: local
    driver_opts:
      type: none
      device: /opt/docker/project/mysql